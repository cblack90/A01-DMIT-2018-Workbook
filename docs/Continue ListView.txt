Continue on Code-First Workbook

ListView has multiple events that we can hook into, this will help with exception handling

F9 will set a break point.

$ before string means string interpolation.  Allows you to embed your actual values of variables right in the your string instead of concatenating.

FreeCamp.WebApp - helps with wiring up exception handling
source code github.com/dgilleland -> Message-User-Control
<%@ Register - known as a register directive

DataKeyNames -> control to support Update/Delete
enum - can think of as a named constant